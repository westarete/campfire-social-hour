defmodule CampfireSocialHour.Poller.MeetingsPoller.Mock do
  @moduledoc false
  def fetch_and_update(), do: nil
end

defmodule CampfireSocialHour.Poller.ParticipantsPoller.Mock do
  @moduledoc false
  def fetch_and_update(), do: nil
end
